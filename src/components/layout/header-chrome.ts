import { PX_PAGE_CLASS } from "@/components/layout/layout-classes";

/** Header chrome — fluid horizontal inset via --page-padding-x. */
export const HEADER_EDGE_PADDING_CLASS = PX_PAGE_CLASS;
/** mWeb: flush under header. Desktop: 24px below header chrome. */
export const HEADER_TO_HERO_GAP_CLASS = "max-lg:mt-0 lg:mt-6";

/** Desktop primary nav — 32px horizontal gap between pill links. */
export const MARKETING_HEADER_NAV_GAP_WEB = 32;

export const marketingHeaderNavClass =
  "flex flex-wrap items-center gap-x-8 gap-y-2";
