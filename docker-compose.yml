version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Any additional environment variables needed for the app
      # can be passed here or in a .env file using `env_file: .env`
    restart: unless-stopped
